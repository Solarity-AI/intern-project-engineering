services:
  - type: web
    name: product-review-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    envVars:
      - key: PORT
        value: 10000
      - key: JAVA_VERSION
        value: 17
      - key: OPENAI_API_KEY
        sync: false
    healthCheckPath: /actuator/health
